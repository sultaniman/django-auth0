============
Installation
============

At the command line::

    $ easy_install django-auth0

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-auth0
    $ pip install django-auth0
